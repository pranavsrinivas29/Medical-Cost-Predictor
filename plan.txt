install requirements

git init
dvc init

eda
feature engineering
model experimentation and training
    -mlflow integration
    -hyperparamter tuning
    -model logging into mlflow
evaluation

inference: use all the preprocessors and saved model

CI :
    - unit test 
    - build docker images
    - login to DockerHub
    - push images to repositories


keep same url for hosted ones
add kubeflow
add monitoring (Prometheus or Grafana)